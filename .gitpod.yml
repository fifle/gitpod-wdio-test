image: gitpod/workspace-full

ports:
  - port: 5900
    onOpen: ignore
  - port: 6080
    onOpen: open-preview
  - port: 4444
    onOpen: notify
tasks:
  - name: install wdio to workspace
    init: |
      npm install
  - name: run tests
    command: |
      gp sync-await
      yes | npm run wdio